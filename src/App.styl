.App
  position absolute
  top 0
  left 0
  min-width 100%
  min-height 100%
  font-family Arial, Helvetica, sans-serif
  display flex
  flex-direction column
  &-main
    flex-grow 1
    margin-top 90px
    @media (max-width 800px)
      margin-top 70px
